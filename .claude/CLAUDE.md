## Git Hosting

My projects are hosted on GitLab, not GitHub. Always use `glab` (GitLab CLI) instead of `gh` (GitHub CLI) for all Git hosting operations including merge requests, issues, CI checks, and releases.

Another Claude agent will review your work after you are done. Create high quality work to minimize the work for the reviewing agent. 
